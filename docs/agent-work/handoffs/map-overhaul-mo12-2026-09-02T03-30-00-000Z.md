# Handoff: map-overhaul MO-1 + MO-2 checkpoint

- From: claude
- To: sub-agent (claude-code / explore mode)
- Handed off: 2026-09-02T03:30:00Z
- Completed or current state: Committed `ed2a44d feat: map overhaul MO-1/MO-2 icon slots + terrain washes, funplay_cocos mcp, system test fix`.
  - Terrain radial-gradient washes per cell (plain/road/forest/cave/rift/water/mountain) replace grid borders.
  - Icon-slot system: every map node renders in a 34–44px rounded slot with a placeholder glyph per kind (人/缘/凶/关). Exit nodes overlay the existing PNG.
  - Player marker halo, hover/focus-revealed node labels, focusable slots, legend uses same icon slots.
  - `.mcp.json`: funplay_cocos added at http://127.0.0.1:8765/; cocos-creator (wrong URL) removed.
  - `.gitignore`: pw-mv.log, run-pw.cmd ignored as local artifacts.
  - 2 new tests in `test/game-screen-art.test.tsx` for slot rendering + glyph + title + focus.
  - `test/system.test.ts` updated for new sys_quest_loaded template with `{objective}`.
- Touched files: `.mcp.json`, `src/index.css`, `src/ui/GameScreen.tsx`, `test/game-screen-art.test.tsx`, `test/system.test.ts`, `.gitignore`, `docs/agent-work/active/map-overhaul-design.md`, `docs/plans/map-overhaul/README.md`.
- Verification: `npx vitest run test/system.test.ts test/game-screen-art.test.tsx` — 28/28 pass. `npm run typecheck` — exit 0. `npm run lint`, `npm run build`, `npx playwright test` not run for this checkpoint; sub-agent must run them as part of MO-5.
- Known risks or blockers:
  - `.map-icon-slot:hover + .map-node-label` selector requires DOM-adjacent siblings; current markup puts the label after the slot, but verify the gap element doesn't break the adjacent-sibling combinator.
  - `pointer-events:none` on `.map-node-label` already set; hover/focus reveal may need to be reinforced if the test runner doesn't fire :hover/:focus-visible.
  - The `map-exit-icon` inside the slot now uses `loading="lazy"` + `decoding="async"`; need to confirm `e2e/acceptance-visual.spec.ts` still passes (it asserts `.map-exit-icon` count and bounds).
- Next action: Continue MO-3 → MO-5 per `docs/plans/map-overhaul/README.md` §5. Update the active-claim acceptance criteria + verification status, then run all 5 gates (`typecheck`, `lint`, `vitest`, `build`, `playwright --workers=1`) and write a follow-up handoff with results.
