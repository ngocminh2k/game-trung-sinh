# Shared work ledger

`active/` holds the current owner and bounded scope for a work unit. `handoffs/` is append-only continuation context; it is not a scratchpad. `done/` is optional archival evidence.

Use the commands in `AGENTS.md`. Do not hand-edit another agent's active claim.
