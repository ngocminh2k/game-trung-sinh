# Playtest p13 — Nhà bản đồ học (Bartle Heart/Explorer): "tôi chỉ muốn nhìn thấy cả tấm bản đồ"

- persona: Cartographer — đi bộ mọi con đường, thăm mọi pin, không fast-travel, mặc kệ quest và combat.
- seed/session: p13 / seed `p13-seed` (engine tự chọn system `ink-and-jade`, `sys_explorer`) / balanced / vi
- actions: ~90 thao tác của tôi (telemetry ghi 79 action, 120 step) | final day: 4 | reached: **still playing** — không kết, không chết; dừng khi hết budget.
- path: Làng Thanh Mộc → Chợ Vân Tập → Thousand Herbs Valley (Hành Thương Thị) → Wandering Market (Hắc Phong Sa Mạc) → Nguyệt Ảnh Hồ → Rừng Sương Mù → Hang Phong Ấn → Linh Thú Lĩnh → Tông Vân Ẩn → Thanh Vân Các → Điền Linh Thảo → về Làng (cả 12 region, toàn bộ bằng chân)
- resources: start hp/qi/gold 100/60/60 → end 72/60/60 · min hp 21 · qi và gold **không đổi một điểm nào cả run** (60/60)

## Moments (đánh số theo action của tôi)
- a1–a8 — Vào game, nhân vật ở Làng Thanh Mộc. Mở tab "Đạo đồ" với kỳ vọng bản đồ thế giới → chỉ là danh sách nút pin `#map-pin-<col>-<row>` của **region hiện tại**. Hoảng: không có map tổng ở bất kỳ đâu.
- a12 — Đi bộ từ làng ra chợ; một con đường ven làng **sinh ra region mới** "Nguyệt Ảnh Hồ" trong danh sách pin. Phát hiện lớn đầu tiên: địa lý mở theo đường đi, không phải theo menu. Ghi sổ ngay.
- a28 — Screenshot đầu tiên (p13-28.png): nền tối, chữ trắng, thuần text. Không có bất kỳ hình ảnh bản đồ nào. UI "map" chỉ là hàng nút bấm có tọa độ.
- a35–a40 — Đặt chân vào Hắc Phong Sa: **−8 HP ngay khi vào**, −8 nữa khi bước thêm. Region nguy hiểm tính tiền bằng máu, không cảnh báo trước. Vẫn thích: đây là "độ cao" trên bản đồ của tôi.
- a48–a52 — Hang Phong Ấn: −19 khi vào, **−17 mỗi bước**; Linh Thú Lĩnh: −20 khi vào, −15 mỗi bước, **lúc ra cũng mất −9**. HP chạm đáy 21. Tôi vẽ được "vùng đỏ" trên bản đồ từ đúng một phép thử.
- a58–a63 — **Con đường lãng phí cả ngày**: pin "Hốc ong linh" (3 bước) ở Hành Thương Thị. Đi tới: không có gì. Không text, không item, không region mới — và sau đó các pin lân cận tự đội giá 2→5 bước (Quầy quay 3→8, đấu giá 2→5). Tôi biết nó vô dụng vì nó là pin duy nhất trong grid không nằm trên đường tới một region nào.
- a70 — Nói chuyện với hướng dẫn sa mạc: `HỘP THOẠI` cắt replies còn ~80 ký tự, chỉ thấy câu của mình. Phải gọi `/state?all=1` mới đọc được cả câu ("Mỏng hơn trước. Người ta không để ý, nhưng ta làm hướng dẫn sa mạc bao năm ở Hắc Phong Sa…"). Bấm nút sang trang đoạn hội thoại → **timeout 20s**, overlay `chat-stage` chặn click. Lore không cuộn được = BLOCKER.
- a71–a72 — Dò tìm bản đồ lần cuối: tab "Đạo đồ" chỉ focus lại widget pin; tab "Hệ thống" là bảng quest + ô chat. Chốt: **game không có màn hình bản đồ thế giới nào cả**.
- a73–a78 — Bounce index hai lần (mở "Hệ thống" làm đảo chỉ số; định bấm "Đi tới Vách tĩnh tâm" lại rơi vào Thanh Vân Các). Nhận ra sub-node travel **không đổi HUD location** — chỉ có dấu X đỏ trên grid (nhìn thấy ở screenshot) báo tôi đang ở đâu.
- a75 — Lần đầu tiên thấy field `night=` trên HUD: "Đêm thứ mười hai: 21 ngày" **khi day=3**. Đồng hồ deadline xuất hiện ở action ~75 và tự mâu thuẫn với bộ đếm ngày.
- a83–a89 — Xuống núi về làng: đây là action đầu tiên sau ~50 mà một bước đi làm HUD location đổi. Ghé "Nhà cũ của ngươi": **zero text diễn biến** — nhà cũ của nhân vật chính không có một dòng hồi ức. Nghỉ → day 4, hp 30→72, đêm đếm 21→20.
- a90 — Screenshot cuối (p13-79.png): panel ĐẠO ĐỒ render grid pin hình thoi với X đỏ — về mặt kỹ thuật đây là "bản đồ" duy nhất trong game, và nó chỉ phủ 1 region 5×5.

## Fullerton 5 questions
- Q1 time pressure: **Gần như không có.** ~116 bước đi không tiêu tốn ngày nào; chỉ `Nghỉ` mới đẩy ngày (3 lần nghỉ = 1→4). Deadline "đêm" xuất hiện ở a75 và sai logic ("Đêm thứ mười hai" khi ngày 3). Là người vẽ bản đồ, tôi được thưởng vì lang thang: đi cả thế giới miễn phí. Nếu đây là ý đồ (khám phá không bị phạt thời gian) thì nên nói rõ; còn không, economy bước-chân bị vỡ.
- Q2 broken-root identity: Mờ.废 căn thể hiện qua `stage=0` và chỉ số không nhúc nhích (qi 60, gold 60 suốt 4 ngày dù đi 12 vùng) — nhưng không một dòng text nào nói tôi *là ai* ngoài hệ `sys_explorer`. Persona废 chỉ sống qua các region khóa cửa, không qua UI.
- Q3 decision clarity: Rõ — toll HP của vùng nguy hiểm đo được bằng một lần thử (−8/−19/−20), đủ để quyết định có vào Hang Phong Ấn hay vòng. Mờ — giá "bước" của các cạnh đổi âm ỉ theo sub-node hiện tại (cổng sơn môn 2→6 sau một chuyến Lũy tre), nên không thể dự đoán đường nào rẻ; mọi tính toán trên bản đồ giấy của tôi đều sai sau một ngày.
- Q4 learning curve: Chậm nhất là **mô hình di chuyển**: ~40 action để hiểu rằng pin = tọa độ lưới per-region, chi phí = khoảng cách từ sub-node, HUD location bất biến khi đi sub-node, và region ẩn chỉ spawn từ đường đi cụ thể. Không có một dòng tooltip nào dạy cơ chế này.
- Q5 narrative engagement: staying beat: câu của hướng dẫn sa mạc ở Hắc Phong Sa (chỉ đọc được qua `?all=1`). Reactivity tốt nhất cũng chính là địa lý: đường làng đẻ ra Nguyệt Ảnh Hồ, sub-node rừng đẻ ra Hang Phong Ấn, cầu sơn môn đẻ ra Thanh Vân Các — thế giới **có** phản hồi với người đi bộ. Tệ nhất: Nhà cũ của ngươi — im lặng tuyệt đối.

## Bugs & friction
- [BLOCKER] a70 — Modal `Trò chuyện cùng NPC`: nội dung tràn 1 màn, không cuộn; click nút pager trả `elementHandle.click: Timeout 20000ms … <div class="chat-stage" …> intercepts pointer events`.lore NPC bị khóa. Workaround của tôi: Đóng + `/state?all=1`.
- [BUG] a70 — Lỗi click trên không xuất hiện trong `GET /errors` (mảng rỗng dù control-server đã timeout thật). Error-reporting của harness mù với lỗi pointer-intercept.
- [BUG] a58/a85 — Chi phí cạnh tự đổi không giải thích: 3→8, 2→5, 2→6 sau khi thăm pin khác. Nếu là "khoảng cách từ sub-node" thì người chơi không có bản đồ cạnh để kiểm chứng — cảm giác như random.
- [BUG] a86 — Pin "Nhà cũ của ngươi" khi tới không sinh bất kỳ THÔNG BÁO/ĐIỄN BIẾN nào.
- [CONFUSION] a71–a72 — Tab "Đạo đồ" hứa một tấm bản đồ, thực chất focus lại widget pin region; không có world map ở mọi màn.
- [CONFUSION] a75 — `night=` chỉ xuất hiện sau ~75 action và không khớp `day=` ("Đêm thứ mười hai" khi day 3, giảm 1 mỗi lần Nghỉ).
- [CONFUSION] a73/a78 — Index đảo sau mỗi action (mở tab Hệ thống) gây bấm nhầm 2 lần; nhân sĩ label "Đi N bước tới X" nhưng N không phải thời gian cố định.
- [NIT] `HỘP THOẠI` cắt NPC reply ~80 ký tự trong digest thường.
- [NIT] Nghỉ trong vùng nguy hiểm hồi ít hơn ngoài vùng an toàn (−8 entry vs full heal) — hợp lý nhưng không có chữ nào giải thích.

## Errors seen
- from /errors: `{"errors":[]}` — rỗng, dù tôi đã hứng 1 lỗi click-timeout thật (xem BLOCKER a70). Chính phát hiện này đáng chú ý hơn cả lỗi.

## Telemetry
- endingId: null · finalDay: 4 · corrections: 0 · nightForgotten: false · steps: 120 (116 move, 3 rest, 1 accept_quest) · hp: 100 → min 21 → 72 · qi: 60 (flat) · gold: 60 (flat) · regions chạm chân: 12/12 (village, market, thousand_herbs_valley, wandering_market, blackwind_dunes, moon_lake, misty_forest, sealed_cave, spirit_beast_ridge, sect, azure_pavilion, herb_field) · survey: null (không có màn ending nên không điền).
- File: docs/playtest-ai/telemetry/p13.json

## Verdict
- clarity 5/10 · agency 7/10 · readability of HUD 4/10 · would-play-again 7/10
- Vì: thế giới này *thực sự* vẽ được bằng chân — region ẩn mở đúng chỗ, vùng nguy hiểm tự đánh dấu bằng máu — nhưng không có lấy một tấm bản đồ, đồng hồ ngày/đêm nói dối, và mỗi con đường đổi giá sau lưng tôi. Nhà bản đồ học vui, nhưng phải vẽ lại bản đồ mỗi sáng.
